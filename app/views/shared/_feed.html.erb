<div class="checkRadioContainer">
	<% if @feed_items.any? %>
	  <%= render @feed_items %>
	  <%= will_paginate @feed_items %>
	<% end %>
</div>

<div class="checkRadioContainer">
<% if @top_goals.any? %>
  <%= render @top_goals %>
  <%= will_paginate @top_goals %>
<% end %>
</div>